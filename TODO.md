# TODO

## matrix

- [ ] `try_mul` — безопасный вариант матричного умножения, возвращающий `Result` при несовпадении размерностей
- [ ] `Display` для `Matrix<T>` — вывод матрицы в читаемом виде (`fmt::Display`)
- [ ] `Index` / `IndexMut` или метод `get(i, j)` — доступ к отдельным элементам матрицы
- [ ] Валидация в `from_data` — строки могут быть разной длины, нужно решить стратегию:
  - вариант 1: проверять что все строки одинаковой длины, иначе ошибка
  - вариант 2: брать наибольшую длину и дополнять остальные нулями (`T::default()`)
  - вариант 3: оба варианта — strict (`try_from_data`) и permissive (`from_data_padded`)
